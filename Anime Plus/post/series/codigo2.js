document.querySelectorAll("span.text-white").forEach(span => { const [year, month, day] = span.textContent.split("-"); if (year && month && day) { span.textContent = new Date(year, month - 1, day).toLocaleDateString("es-ES", { year: "numeric", month: "long", day: "numeric" }); } });document.addEventListener("DOMContentLoaded", function() { const generosContainer = document.getElementById("btng"); generosContainer.innerHTML = generosContainer.textContent .split(",") .map(genero => `<span class="bg-gray-700 text-sm text-white rounded-full px-2 py-1 mr-1">${genero}</span>`) .join(""); }); <
